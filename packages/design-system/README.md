# Design System

Designsystemet kommer innehålla färger, typografi, komponentprinciper, statusnivåer och riskindikatorer.

I v0.2 ligger tokens och CSS i `apps/pwa/src/styles.css`. Senare bryts detta ut hit.
